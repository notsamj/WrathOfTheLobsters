# Optional
myDomain = 50
myRange = 50

degree = 5
# TODO: Create polynomial function with random constants

# TODO: Calculate derivative

# TODO: Use bisection? to locate local maximum and minimum by finding where derivative = 0

# Keep generating sets of constants until max(abs(minValue), maxValue) < myRange

# Import this into the game with constants can be in range(0, constant) or (constant, 0)